--- linux-6.6.51/drivers/watchdog/sama5d4_wdt.c	2024-12-05 13:07:55.231023400 +0900
+++ linux4microchip-fpga-2024.09/drivers/watchdog/sama5d4_wdt.c	2024-12-04 16:41:35.682501200 +0900
@@ -254,7 +254,6 @@
 	struct sama5d4_wdt *wdt;
 	void __iomem *regs;
 	u32 irq = 0;
-	u32 reg;
 	int ret;
 
 	wdt = devm_kzalloc(dev, sizeof(*wdt), GFP_KERNEL);
@@ -305,12 +304,6 @@
 
 	watchdog_init_timeout(wdd, wdt_timeout, dev);
 
-	reg = wdt_read(wdt, AT91_WDT_MR);
-	if (!(reg & AT91_WDT_WDDIS)) {
-		wdt->mr &= ~AT91_WDT_WDDIS;
-		set_bit(WDOG_HW_RUNNING, &wdd->status);
-	}
-
 	ret = sama5d4_wdt_init(wdt);
 	if (ret)
 		return ret;
